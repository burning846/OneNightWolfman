version: "3.9"

services:
  wolf-server:
    build: ./server
    restart: unless-stopped
    ports:
      - "4000:4000"
    environment:
      PORT: 4000
      # 生产环境务必填上前端域名，逗号分隔多个
      # CORS_ORIGIN: "https://your-app.vercel.app,https://wolf.example.com"
      CORS_ORIGIN: ""
